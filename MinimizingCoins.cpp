#include <bits/stdc++.h>
using namespace std;
long long int solve(long long int n, long long int x, long long int coins[]){
    
}
int main()
{
    long long int n, x;
    cin >> n >> x;
    long long int coins[n];
    for (int i = 0; i < n; i++)
        cin >> coins[i];
    cout << solve(n, x, coins);
}